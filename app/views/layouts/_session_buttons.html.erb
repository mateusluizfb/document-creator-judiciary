<div class="pull-right">

  <% if controller.controller_name == 'judiciary_process' && controller.action_name == 'new'%>
    <%= link_to 'Voltar', processes_path, method: :get, class: 'btn btn-warning navbar-btn'  %>
  <% end %>

  <% if user_signed_in? %>
    <%= link_to 'Logout', destroy_user_session_path, method: :delete, class: 'btn btn-danger navbar-btn'  %>
  <% end %>

</div>
